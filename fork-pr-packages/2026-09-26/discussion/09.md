# feat(execution-policy): verify terminal delivery evidence

Unsent discussion draft. The operator must decide whether and where to post it.

**Problem**

A code task can claim completion without verifying the repository head, pull request and required checks.

**Proposed outcome**

Persist provider verification receipts and bind terminal decisions to the submitted work products.

**Existing work and scope**

PR 11196 is open and its original commits are retained. Recommend fold into that work or coordinate a credited successor. ROADMAP identifies enforced outcomes as core; maintainer discussion is not established.

**Decision needed before a PR**

Open PR 11196 overlaps; required checks, provider-neutral coverage and native terminal path do not meet record; malformed evidence can pass.

**Acceptance still required**

Needs malformed payload, missing required context, native arbiter, target-race and provider-neutral real-persistence proofs before opening.

**Compatibility concerns**

Current malformed200 payload handling can pass with incomplete evidence. Configured required checks are not discovered. Native arbiter bypass and GitHub-only scope differ from the required outcome. Migration0284 collides with 07.
