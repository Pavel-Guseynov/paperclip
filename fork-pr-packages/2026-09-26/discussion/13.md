# chore(toolchain): migrate to pnpm 11

Unsent discussion draft. The operator must decide whether and where to post it.

**Problem**

The intended pnpm 11 toolchain must be selected consistently in local, CI, packaging and container consumers.

**Proposed outcome**

Move pnpm configuration into the workspace manifest and update consumers and policy checks.

**Existing work and scope**

Issue 8827 remains relevant. PR 13894 is the superseded stable-branch submission. A maintainer-owned trusted-toolchain and bot-refresh sequence is required.

**Decision needed before a PR**

Manual lockfile edits are forbidden; trusted master workflow pins pnpm 9; current lockfile merge conflicts.

**Acceptance still required**

No clean permitted contribution sequence is established. Do not drop the lockfile or bypass its policy.

**Compatibility concerns**

The trusted workflow ignores the PR copy when selecting its bootstrap toolchain. The patch-manifest parser is duplicated in packaging and provisioning.
