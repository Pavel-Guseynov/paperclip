> **Submission status: blocked.** Logging defects fixed; regression, policy checks, typecheck and build pass. Full suites, coverage and remaining PR lanes are unverified. Do not open this draft until the missing proof and decisions below are resolved.

## Thinking Path

> - Paperclip manages AI agents and their work.
> - This change concerns gateway token log redaction.
> - Gateway credentials can reach request logs, error fields, free text and child logger bindings.
> - The behavior must preserve existing authorization, data and lifecycle contracts.
> - This branch aims to use one credential policy across HTTP serializers and native Pino hooks, censor deep uninspected subtrees, and sanitize final serialized bindings.
> - The review found the unresolved limits in Verification and Risks below.

## Linked Issues or Issue Description

Refs #4759

Refs #10784

[PR #10784](https://github.com/paperclipai/paperclip/pull/10784) is open and adds two unsupported-auth header paths. This branch covers a broader gateway logging boundary. A separate focused PR is justified with attribution; it must not claim the small header fix is novel.

## What Changed

- `server/src/__tests__/http-log-redaction.test.ts`: Serialized sentinel matrix.
- `server/src/middleware/http-log-redaction.ts`: Shared credential names and text/error/record redaction.
- `server/src/middleware/logger.ts`: Pino hooks and serializers.
- `server/src/middleware/redact-sensitive.ts`: Reuse common credential authority.

Fixed deep traversal returning uninspected credential subtrees and Pino child/setBindings metadata escaping the log method hook. The existing field policy now sanitizes the native serialized sink. Malformed serialized output emits a constant safe record.

Dependency: no separate contribution dependency declared.

## Verification

Upstream base: `4ca404b49ab3ec5513b9cfa824ff2eeaef941f7a`.
Contribution head: `e2809924bfe1736b1be359f3a1ef6fc804c3db8f`.
Current upstream contained: yes.

An identical real HTTP/Pino test fails on exact current upstream because the gateway header reaches output, then passes on the final head. Six additional tests fail on the pre-correction contribution and all 100 pass after correction; 95 stable and 29 adjacent stable tests pass, 62 gateway tests skip. All executed policy checks and 474 policy-script tests, recursive typecheck and workspace build pass. Full suites and 100% statement coverage remain unverified.

Executed: `pnpm exec vitest run server/src/__tests__/http-log-redaction.test.ts` (100 passed); identical HTTP proof fixture (upstream 1 failed, head 1 passed); policy checks P02–P15 (474 script tests passed in total); `pnpm typecheck` and `pnpm build` (exit 0). Full test groups, remaining PR lanes and 100% coverage are not proved. Warnings are retained.

Before submission, complete the remaining full test, release, Runner, Docker and browser lanes. Keep the recorded typecheck and build evidence tied to this head. Database setup failed before assertions in an adjacent base test; discarded initdb stderr prevents a deeper diagnosis. No CI or Greptile result exists for this unopened PR.

## Risks

Deep diagnostic fields are omitted. The final serialized-record scan adds logging work.

Logging defects fixed; regression, policy checks, typecheck and build pass. Full suites, coverage and remaining PR lanes are unverified.

For core feature work, check ROADMAP.md and discuss the proposal in #dev before opening. This audit read the roadmap but did not send messages or obtain a maintainer decision.

## Model Used

The previous author package identifies Anthropic Claude Opus 5.5 (`claude-opus-5-5`). That is inherited provenance, not a new runtime verification.

This review and the 2026-09-26 corrections used OpenAI Codex, GPT-6 family, with code inspection, tools, code execution and collaborating agents. The exact served model identifier and context-window size are not exposed in this session and are not invented here. No different model selection was requested.

## Checklist

- [x] I have included a thinking path that traces from project context to this change
- [ ] I have specified the model used (with version and capability details)
- [ ] I have checked ROADMAP.md and confirmed this PR does not duplicate planned core work
- [x] I have searched GitHub for duplicate or related PRs and linked them above
- [x] I have either (a) linked existing issues with `Fixes: #` / `Closes #` / `Refs #` OR (b) described the issue in-PR following the relevant issue template
- [x] I have not referenced internal/instance-local Paperclip issues or links (only public GitHub `#NNN` / `github.com/paperclipai/paperclip` URLs)
- [x] My branch name describes the change (e.g. `docs/...`, `fix/...`) and contains no internal Paperclip ticket id or instance-derived details
- [ ] I have run tests locally and they pass
- [x] I have added or updated tests where applicable
- [ ] I have updated relevant documentation to reflect my changes
- [x] I have considered and documented any risks above
- [ ] All Paperclip CI gates are green
- [ ] Greptile is 5/5 with no open P2s, recommendations, or follow-ups
- [ ] I will address all Greptile and reviewer comments before requesting merge
