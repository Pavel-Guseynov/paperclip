> **Submission status: blocked.** Missing environments and fallback paths can report false cleanup; live/native guards and bounded sweep progress are incomplete. Do not open this draft until the missing proof and decisions below are resolved.

## Thinking Path

> - Paperclip manages AI agents and their work.
> - This change concerns release terminal run leases.
> - A cancelled or interrupted run can retain an environment lease after provider shutdown.
> - The behavior must preserve existing authorization, data and lifecycle contracts.
> - This branch aims to release terminal leases only after authoritative provider cleanup and absent live execution ownership.
> - The review found the unresolved limits in Verification and Risks below.

## Linked Issues or Issue Description

Refs #13515

[PR #13515](https://github.com/paperclipai/paperclip/pull/13515) already added bounded active lease recovery on development master; stable tag lacks it. Stable's added sweep includes an upstream backport and must not be claimed as original. No released equivalent of the complete outcome proven. Retained service logs also show a server lifecycle transition between the recorded historical run start and cancellation; the earlier inference of uninterrupted process ownership is not established.

## What Changed

- `server/src/__tests__/release-cancelled-interrupted-run-leases.test.ts`: Intended cancellation/recovery/reconciliation coverage; does not reproduce the real in-process leak.
- `server/src/services/environment-runtime.ts`: Handles absent environment context, currently by fabricating successful local cleanup.
- `server/src/services/environments.ts`: Extends bulk release to pending-cleanup leases and cleanup/failure options.
- `server/src/services/heartbeat.ts`: Adds cancellation release and stranded-lease recovery guards/shortcuts.
- `server/src/services/recovery/service.ts`: Adds lease-release dependency after orphan terminalization, with unsafe bulk fallback.

This review did not add a source correction beyond upstream synchronization where clean. The listed behavior still needs the proof below.

Dependency: no separate contribution dependency declared.

## Verification

Upstream base: `4ca404b49ab3ec5513b9cfa824ff2eeaef941f7a`.
Contribution head: `46e8948a8df775fa11e3d48e070a20188c91d61f`.
Current upstream contained: no; resolve the recorded merge conflict before submission.

The historical in-process cancel cause remains unobservable: no retained executeRun/finally trace proves it. Needs real cancellation settlement, provider cleanup failures, native/live ownership, missing environment and sweep progress regressions.

No full gate or matching base/head regression result is claimed for this final head.

Before submission, complete the declared local typecheck, full test suite and build, plus applicable policy, release, Runner, Docker and browser lanes. Database setup failed before assertions in an adjacent base test; discarded initdb stderr prevents a deeper diagnosis. No CI or Greptile result exists for this unopened PR.

## Risks

Current code fabricates archived/local success for missing environments and bulk-releases remote/pending-cleanup leases. Guarded oldest rows may starve a bounded page.

Missing environments and fallback paths can report false cleanup; live/native guards and bounded sweep progress are incomplete.

For core feature work, check ROADMAP.md and discuss the proposal in #dev before opening. This audit read the roadmap but did not send messages or obtain a maintainer decision.

## Model Used

The previous author package identifies Google DeepMind Gemini 3.8 Flash (High), via Antigravity. An exact API identifier was not supplied there.

This review and the 2026-09-26 corrections used OpenAI Codex, GPT-6 family, with code inspection, tools, code execution and collaborating agents. The exact served model identifier and context-window size are not exposed in this session and are not invented here. No different model selection was requested.

## Checklist

- [x] I have included a thinking path that traces from project context to this change
- [ ] I have specified the model used (with version and capability details)
- [ ] I have checked ROADMAP.md and confirmed this PR does not duplicate planned core work
- [x] I have searched GitHub for duplicate or related PRs and linked them above
- [x] I have either (a) linked existing issues with `Fixes: #` / `Closes #` / `Refs #` OR (b) described the issue in-PR following the relevant issue template
- [x] I have not referenced internal/instance-local Paperclip issues or links (only public GitHub `#NNN` / `github.com/paperclipai/paperclip` URLs)
- [x] My branch name describes the change (e.g. `docs/...`, `fix/...`) and contains no internal Paperclip ticket id or instance-derived details
- [ ] I have run tests locally and they pass
- [x] I have added or updated tests where applicable
- [ ] I have updated relevant documentation to reflect my changes
- [x] I have considered and documented any risks above
- [ ] All Paperclip CI gates are green
- [ ] Greptile is 5/5 with no open P2s, recommendations, or follow-ups
- [ ] I will address all Greptile and reviewer comments before requesting merge
