> **Submission status: blocked.** Depends on the proposal “fix(codex): authenticate managed MCP lifecycle requests”; database lifecycle tests and complete current-head gates are unverified. Do not open this draft until the missing proof and decisions below are resolved.

## Thinking Path

> - Paperclip manages AI agents and their work.
> - This change concerns gateway session-token verification.
> - Session endpoints can reject a session bearer or confuse it with the managed gateway token family.
> - The behavior must preserve existing authorization, data and lifecycle contracts.
> - This branch aims to hand off only the session token family on session paths and reject the gateway token family there.
> - The review found the unresolved limits in Verification and Risks below.

## Linked Issues or Issue Description

Refs #13140

Refs #12518

[Issue #13140](https://github.com/paperclipai/paperclip/issues/13140) is open but discusses an adjacent gateway-token path. Use Refs rather than claiming full closure. [PR #12518](https://github.com/paperclipai/paperclip/pull/12518) overlaps dependency01. Separate session-token increment after 01 is justified.

## What Changed

- `doc/MCP-ACCESS-GOVERNANCE.md`: Session bearer usage contract.
- `doc/MCP-DEMO-SCRIPT.md`: Session bearer example.
- `packages/adapters/codex-local/src/server/codex-home.test.ts`: Dependency: “fix(codex): authenticate managed MCP lifecycle requests”: Generated MCP authentication configuration assertions.
- `server/src/__tests__/agent-auth-middleware.test.ts`: Preserve ordinary actor authentication checks.
- `server/src/__tests__/codex-local-execute.test.ts`: Dependency: “fix(codex): authenticate managed MCP lifecycle requests”: Adapter execution configuration assertions.
- `server/src/__tests__/tool-gateway.test.ts`: Session issuance, rejection, and lifecycle coverage.
- `server/src/lib/uuid.ts`: Dependency: “fix(codex): authenticate managed MCP lifecycle requests”: Shared UUID text shape, also used by three audit filters.
- `server/src/middleware/auth.ts`: Token-family-gated session handoff.
- `server/src/routes/tool-gateway.ts`: Session bearer extraction.
- `server/src/services/tool-gateway.ts`: Reject named gateway token family on session endpoints.

Inherited the managed MCP authentication proposal's test-fixture correction by ordinary merge.

Dependency: fix(codex): authenticate managed MCP lifecycle requests (open and review first)

## Verification

Upstream base: `4ca404b49ab3ec5513b9cfa824ff2eeaef941f7a`.
Contribution head: `967b5a6cc73a86be8552e0be6e4eee964574076d`.
Current upstream contained: yes.

32/32 middleware tests passed on the sync head; 54/54 middleware+execute tests passed after fixture correction. the session-token stable line and main touched execute suite21/21 passed. Real session lifecycle and complete gates remain unverified.

Executed: `pnpm exec vitest run server/src/__tests__/agent-auth-middleware.test.ts server/src/__tests__/codex-local-execute.test.ts` (54 passed). This is not the full test suite.

Before submission, complete the declared local typecheck, full test suite and build, plus applicable policy, release, Runner, Docker and browser lanes. Database setup failed before assertions in an adjacent base test; discarded initdb stderr prevents a deeper diagnosis. No CI or Greptile result exists for this unopened PR.

## Risks

Ordinary actor authentication must retain its scope and token failures must fail closed.

Depends on the proposal “fix(codex): authenticate managed MCP lifecycle requests”; database lifecycle tests and complete current-head gates are unverified.

For core feature work, check ROADMAP.md and discuss the proposal in #dev before opening. This audit read the roadmap but did not send messages or obtain a maintainer decision.

## Model Used

The previous author package identifies Anthropic Claude Opus 5.5 (`claude-opus-5-5`). That is inherited provenance, not a new runtime verification.

This review and the 2026-09-26 corrections used OpenAI Codex, GPT-6 family, with code inspection, tools, code execution and collaborating agents. The exact served model identifier and context-window size are not exposed in this session and are not invented here. No different model selection was requested.

## Checklist

- [x] I have included a thinking path that traces from project context to this change
- [ ] I have specified the model used (with version and capability details)
- [ ] I have checked ROADMAP.md and confirmed this PR does not duplicate planned core work
- [x] I have searched GitHub for duplicate or related PRs and linked them above
- [x] I have either (a) linked existing issues with `Fixes: #` / `Closes #` / `Refs #` OR (b) described the issue in-PR following the relevant issue template
- [x] I have not referenced internal/instance-local Paperclip issues or links (only public GitHub `#NNN` / `github.com/paperclipai/paperclip` URLs)
- [x] My branch name describes the change (e.g. `docs/...`, `fix/...`) and contains no internal Paperclip ticket id or instance-derived details
- [ ] I have run tests locally and they pass
- [x] I have added or updated tests where applicable
- [ ] I have updated relevant documentation to reflect my changes
- [x] I have considered and documented any risks above
- [ ] All Paperclip CI gates are green
- [ ] Greptile is 5/5 with no open P2s, recommendations, or follow-ups
- [ ] I will address all Greptile and reviewer comments before requesting merge
