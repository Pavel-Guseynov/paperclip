# 01 — Managed MCP lifecycle authentication

**Blocked for submission; scoped fix prepared, committed and pushed to the fork.** The focused real-database tests pass. Broader gates failed and main verification is blocked by dependency synchronization. No upstream submission or message was made.

## Remaining scope and upstream status

The operator requested that already-merged behavior be excluded. The contribution now covers managed MCP lifecycle authentication only. The Codex `http_headers` fix is already in upstream master through [PR 13942](https://github.com/paperclipai/paperclip/pull/13942), commit `18dac1e1efa48477df83b88ae9d8dbf134438e5e`. Its implementation and redundant header-test changes are absent from this contribution diff. Stable retains the header fix and its coverage because that release still needs them.

[Issue 10346](https://github.com/paperclipai/paperclip/issues/10346) reports the related problem. [Draft PR 12518](https://github.com/paperclipai/paperclip/pull/12518), by **callisto-syn**, overlaps the managed authentication path. It is another contributor's draft; it is not this fork branch. Credit is preserved in the proposed body. The operator authorized preparing the narrower contribution, so scope preparation no longer needs a decision. No coordination with that author is claimed.

The compared master still fails the causal auth regressions. The remaining outcome is not a retirement candidate. No released-equivalence claim is made. Public status is the inspected 2026-09-26 snapshot.

## Exact branch state

| Ref | Full head, copied from git rev-parse |
| --- | --- |
| Contribution `fix/codex-managed-mcp-auth` and origin | `d7f732a84310958de14f5a02f859cd4e797cb48c` |
| Stable `stable/v2026.916.0/fix/codex-managed-mcp-auth` and origin | `aa542ea3d1c487c9725314a230581d6042269986` |
| Fork `main` and origin | `e0e4336bea70cce0e925593dbaaa55e6d58f3a8e` |
| Compared `upstream/master` | `7f3c06dac4604dddcf870085f1a623c102261358` |

A fresh `git ls-remote origin` confirmed all three pushed heads. The contribution contains the compared upstream. No base merge was imported into stable/main. Branch names and history were preserved. There is no separate contribution dependency. Change 02 will need the latest 01 ancestry considered when the operator proceeds to 02; its separate refs were not changed in this focused follow-up.

## Complete contribution diff

`git diff 7f3c06dac4604dddcf870085f1a623c102261358...d7f732a84310958de14f5a02f859cd4e797cb48c`: six files, 536 insertions, 11 deletions.

| File | Rationale |
| --- | --- |
| `README.md` | Documents managed gateway credentials, notification responses and no-write/no-double-charge behavior; links the operator command contract. |
| `server/src/middleware/auth.ts` | Hands only managed POST requests with pcgw_ bearers to gateway verification; preserves the public endpoint actor and ordinary authentication elsewhere. |
| `server/src/routes/tool-gateway.ts` | Authenticates initialized notifications and returns empty 202, typed rejection status, or 500. |
| `server/src/services/tool-gateway.ts` | Reuses token/run checks without charging another protocol action or writing successful-notification usage/identity. |
| `server/src/__tests__/tool-gateway.test.ts` | Real PostgreSQL and HTTP regressions across both deployment modes, route boundaries, lifecycle invalidation, counters, audit redaction and database failure. |
| `server/src/__tests__/codex-local-execute.test.ts` | Four fixture HOME setup/restore lines prevent the two resume tests touching host authentication files. |

The unrelated generic UUID helper/audit-filter refactor was removed. Added middleware-only fake-DB tests were replaced by real PostgreSQL boundary coverage. No schema, dependency, lockfile, configuration or migration change remains.

## Review and executed behavioral proof

The managed exception accepts only POST to the exact UUID endpoint with a gateway bearer. It clears implicit board authority and leaves actual authentication to the gateway service. Descriptor GET, malformed IDs, lookalike suffixes, unrelated routes and non-gateway bearers retain actor authentication. Generic database-valid UUIDs are accepted. Public gateway actor behavior matches upstream.

Notifications verify gateway/token state, secret hash and run/agent/company/context constraints. Success neither writes usage/identity nor charges a second protocol action. Invalid credentials still reach failure throttling and redacted audit. The real closed-client regression proves an unexpected database error returns an empty 500 and does not break the shared fixture.

Final focused command:

```sh
pnpm exec vitest run server/src/__tests__/tool-gateway.test.ts server/src/__tests__/agent-auth-middleware.test.ts server/src/__tests__/codex-local-execute.test.ts
```

**186 passed, three files, zero skipped**, on the exact source tree committed as the contribution head. Server typecheck also passed after this final test-only refactor.

For the causal comparison, the three changed production files were temporarily restored exactly to upstream, retaining the final test/README overlay. `git diff upstream/master --name-only` confirmed only README and the two test files differed. The identical targeted auth command produced **6 failures / 137 skipped** in the 143-test gateway file: valid managed initialize returned 401 instead of 200 in both modes; tampered public notification returned 202 instead of 401 in both modes; managed rejection had the ordinary actor JSON error body instead of an empty gateway response in both modes. These are three checked behaviors around the remaining auth defects. The implementation was then restored and `git diff --exit-code` passed.

This replaces the earlier fixture's HTML-body baseline observation with the real error middleware's JSON response. Complete causal and passing outputs: [final baseline](../evidence/01-focused/gateway-final-base.log), [final head](../evidence/01-focused/focused-final.log).

## Gates and limits

[Verification inventory](../evidence/01-focused/verification.md) records every executed command, its tested revision, result and retained evidence. The broad gates ran on `bed8d5c5f80d7b756626c244f187ec12b06e1340`; only the two test files changed afterward. Broad results are not mislabeled as executions of the final commit.

- Passing: recursive typecheck, workspace build, build-gap checks, policy checks and 460 policy tests, 129 release-registry tests, Runner eval checks, and 1,742 API authority tests.
- Root `pnpm test:run` exited 1: first phase 13,594 passed / 8 failed / 80 skipped. Later workspace groups and 148 serialized suites were not reached.
- Runner aggregate: 2,087 passed / 1 failed / 10 skipped after 38 preparation passes. Rust independently: 293 passed / 1 failed in its core library binary; later targets were not completed.
- Browser CI root union: 139 passed / 2 failed / 4 skipped.
- Docker executable unavailable. Resolution-only CI install/canary preparation and post-open CI/Greptile remain unverified. 100% statement coverage was not demonstrated.

The failed broader tests were not run on the matching upstream base; none is called pre-existing. Complete diagnoses distinguish observed causes from missing diagnostics. No unrelated gate fixes were folded into 01.

## Stable and main integration

Shared fixes reached stable by ordinary `cherry-pick -x`:

| Contribution commit | Stable commit |
| --- | --- |
| `9b1ac5dd565e59db363500fdf645f0e4bbb474e0` | `b6477fd6eb4c6db87f9fa16c36a1affaf95275e0` |
| `9044c3df1abf7f523a142f3aa795472ff0246cf1` | `1f075900786827b9168ab1208b37f40438c2d5f9` |
| `de4aaa7c8087a8a6c2432c027b8d33c0d82f8af2` | `bb51e9faba73bfff8a89d26d15187e052f901fbc` |
| `d7f732a84310958de14f5a02f859cd4e797cb48c` | `aa542ea3d1c487c9725314a230581d6042269986` |

Upstream-only merge `77c0589937a2d61e5f699d05879fc1fa90d65f8d` and redundant header-test cleanup `bed8d5c5f80d7b756626c244f187ec12b06e1340` were excluded from stable.

Stable's four touched gateway/actor/Codex test files passed **224/224**, zero skipped. [Log](../evidence/01-focused/stable-focused.log).

Main merged stable through `e0e4336bea70cce0e925593dbaaa55e6d58f3a8e`. Conflict resolution preserved Change 02's independent session-route actor handoff and its existing custom-header/Bearer verifier, constant-time hash check, and expiry/revocation/run constraints. The Change 02 session regression tests remain. The old broad session-route exemption is unchanged and belongs to 02's later review.

**Packaging behavior change:** managed descriptor requests no longer use the gateway POST exemption; initialized notifications do not double-charge or write usage/identity; the public path preserves the deployment default actor. No schema or configuration migration is required.

Main's test command did not start Vitest. pnpm's automatic dependency check attempted installation and aborted module-directory removal without a TTY. Native `pnpm install --frozen-lockfile` was then rejected by automatic approval review because installed dependency mutation is prohibited by the supplied repository instructions. No install bypass, confirmation override, dependency edit, or lockfile change was made. [Original command output](../evidence/01-focused/main-focused.log). Main is pushed but **unverified for this merge**, not certified for deployment.

## Operator package and stopping point

Title: **fix(server): authenticate managed MCP lifecycle requests**.

[Template body](../pr/01.md), [verification](../evidence/01-focused/verification.md), [exact state](../evidence/01-focused/final-state.json). Existing public issue references satisfy the issue-description path; no new issue draft is required.

Preparation is complete to the documented blocker boundary. Do not open this draft as green. The operator requested a stop after 01; other change work is not continuing. Packaging records were not edited. The packaging session should record these heads, narrowed scope, passing focused/stable tests and remaining gate/main-verification blockers.
