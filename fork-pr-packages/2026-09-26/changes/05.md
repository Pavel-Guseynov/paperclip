# 05 — Gateway token log redaction

**blocked.** Logging defects fixed; regression, policy checks, typecheck and build pass. Full suites, coverage and remaining PR lanes are unverified.

## Upstream status

[PR #10784](https://github.com/paperclipai/paperclip/pull/10784) is open and adds two unsupported-auth header paths. This branch covers a broader gateway logging boundary. A separate focused PR is justified with attribution; it must not claim the small header fix is novel.

No released equivalent with a passing regression was established. This is not a retirement recommendation. Searches establish the linked public findings, not an exhaustive proof that no related work exists. Detailed source and upstream evidence: [audit](../evidence/review-01-05-14-16.md), [public overlap supplement](../evidence/upstream-supplement.md).

## Exact branch state

| Ref | Full head |
| --- | --- |
| Contribution `fix/tool-gateway-token-log-redaction` | `e2809924bfe1736b1be359f3a1ef6fc804c3db8f` |
| Origin contribution | `e2809924bfe1736b1be359f3a1ef6fc804c3db8f` |
| Stable `stable/v2026.916.0/fix/tool-gateway-token-log-redaction` | `e42ecc4c8c7161fc58c5a1012d294a558a6d9e28` |
| Origin stable | `e42ecc4c8c7161fc58c5a1012d294a558a6d9e28` |
| Fetched upstream master | `4ca404b49ab3ec5513b9cfa824ff2eeaef941f7a` |

All heads were read with git rev-parse; final origin identities also matched a fresh git ls-remote result. Changed and pushed during this audit; exact command/backport logs are linked below.

Current upstream contained: **yes**. Every stable line already contains v2026.916.1 (`d554c4789ed3930f8a53ac9fdf6503b3187097da`), including lines whose fixed names say .0. [Synchronization disposition](../evidence/synchronization.md).

Dependencies: None declared.

No separate contribution dependency is declared. The fixed branch identity and existing history are preserved. If upstream requests a different commit shape, the operator must discuss that requirement; this audit did not squash, reset or rewrite it.

## Current merge-base diff and rationale

Inventory command: `git diff --name-status 4ca404b49ab3ec5513b9cfa824ff2eeaef941f7a...e2809924bfe1736b1be359f3a1ef6fc804c3db8f`. On a branch missing current upstream this remains an older-merge-base diff and is not a clean current-base acceptance claim.

| File | Rationale |
| --- | --- |
| `server/src/__tests__/http-log-redaction.test.ts` | Serialized sentinel matrix. |
| `server/src/middleware/http-log-redaction.ts` | Shared credential names and text/error/record redaction. |
| `server/src/middleware/logger.ts` | Pino hooks and serializers. |
| `server/src/middleware/redact-sensitive.ts` | Reuse common credential authority. |

## Review outcome

Problem: Gateway credentials can reach request logs, error fields, free text and child logger bindings.

Current intended implementation: Use one credential policy across HTTP serializers and native Pino hooks, censor deep uninspected subtrees, and sanitize final serialized bindings.

Fixed deep traversal returning uninspected credential subtrees and Pino child/setBindings metadata escaping the log method hook. The existing field policy now sanitizes the native serialized sink. Malformed serialized output emits a constant safe record. All corrections and nine earlier review backports reached stable/main.

Remaining findings and compatibility risks: Deep diagnostic fields are omitted. The final serialized-record scan adds logging work. Stable-only API response sanitization was deliberately removed by the earlier review backports.

Required next proof: An identical real HTTP/Pino test fails on exact current upstream because the gateway header reaches output, then passes on the final head. Six additional tests fail on the pre-correction contribution and all 100 pass after correction; 95 stable and 29 adjacent stable tests pass, 62 gateway tests skip. All executed policy checks and 474 policy-script tests, recursive typecheck and workspace build pass. Full suites and 100% statement coverage remain unverified.

## Regression and touched-line verification

The identical HTTP proof fails1/1 on current upstream and passes 1/1 on this head. Six added tests fail on the older contribution and pass after the correction. See [final verification](../evidence/05/current-head-verification.md) and [stable backport mapping](../evidence/05/README.md).

An identical real HTTP/Pino test fails on exact current upstream because the gateway header reaches output, then passes on the final head. Six additional tests fail on the pre-correction contribution and all 100 pass after correction; 95 stable and 29 adjacent stable tests pass, 62 gateway tests skip. All executed policy checks and 474 policy-script tests, recursive typecheck and workspace build pass. Full suites and 100% statement coverage remain unverified.


## Full gate matrix

This matrix distinguishes executed outcomes from required commands. Counts N/A mean no test count was produced, not zero failures. Full command expansion and runtime conditions: [verification authority](../evidence/verification-contract.md). No failure is called pre-existing without a matching base execution. Database-dependent proof stopped at failed fixture initialization with missing retained initdb stderr; [baseline output](../evidence/upstream-file-delivery.log).

| Gate | Command / authority | Result and counts |
| --- | --- | --- |
| P01 | Reject pnpm-lock.yaml in `git diff --name-only 4ca404b49ab3ec5513b9cfa824ff2eeaef941f7a...e2809924bfe1736b1be359f3a1ef6fc804c3db8f` unless branch is exactly `chore/refresh-lockfile` or author exactly `dependabot[bot]`. | No pnpm-lock.yaml in the inspected merge-base diff; source policy inspection, not a PR quality execution. |
| P02 | `node .github/scripts/check-pr-migration-order.mjs 4ca404b49ab3ec5513b9cfa824ff2eeaef941f7a e2809924bfe1736b1be359f3a1ef6fc804c3db8f` | PASS; exit 0; test count N/A. [Full output](../evidence/05/P02.log). |
| P03 | `node scripts/check-docker-deps-stage.mjs` | PASS; exit 0; test count N/A. [Full output](../evidence/05/P03.log). |
| P04 | `pnpm check:node-version` | PASS; exit 0; test count N/A. [Full output](../evidence/05/P04.log). |
| P05 | `node scripts/check-no-git-push.mjs` | PASS; exit 0; test count N/A. [Full output](../evidence/05/P05.log). |
| P06 | `node --test scripts/check-no-git-push.test.mjs` | PASS; exit 0; 14 passed, 0 failed, 0 skipped. [Full output](../evidence/05/P06.log). |
| P07 | `pnpm check:module-boundaries` | PASS; exit 0; test count N/A. [Full output](../evidence/05/P07.log). |
| P08 | `node --test scripts/check-module-boundaries.test.mjs` | PASS; exit 0; 3 passed, 0 failed, 0 skipped. [Full output](../evidence/05/P08.log). |
| P09 | `node --test '.github/scripts/tests/*.test.mjs'` | PASS; exit 0; 377 passed, 0 failed, 0 skipped. [Full output](../evidence/05/P09.log). |
| P10 | `node --test scripts/__tests__/run-vitest-stable-shard.test.mjs` | PASS; exit 0; 24 passed, 0 failed, 0 skipped. [Full output](../evidence/05/P10.log). |
| P11 | `node --test scripts/__tests__/e2e-shard.test.mjs` | PASS; exit 0; 11 passed, 0 failed, 0 skipped. [Full output](../evidence/05/P11.log). |
| P12 | `node --test scripts/__tests__/release-verify-workflow.test.mjs scripts/cloud-source-verification.test.mjs scripts/standard-image-contract.test.mjs` | PASS; exit 0; 39 passed, 0 failed, 0 skipped. [Full output](../evidence/05/P12.log). |
| P13 | `node --test scripts/__tests__/build-standalone-concurrency.test.mjs` | PASS; exit 0; 6 passed, 0 failed, 0 skipped. [Full output](../evidence/05/P13.log). |
| P14 | `node scripts/release-package-map.mjs check` | PASS; exit 0; test count N/A. [Full output](../evidence/05/P14.log). |
| P15 | `PAPERCLIP_RELEASE_BOOTSTRAP_BASE_SHA=4ca404b49ab3ec5513b9cfa824ff2eeaef941f7a node scripts/check-release-package-bootstrap.mjs server/src/__tests__/http-log-redaction.test.ts server/src/middleware/http-log-redaction.ts server/src/middleware/logger.ts server/src/middleware/redact-sensitive.ts` | PASS; exit 0; test count N/A. [Full output](../evidence/05/P15.log). |
| P16 | `pnpm install --resolution-only --ignore-scripts --no-frozen-lockfile` | Not run; needs the disposable CI merge tree. Durable lockfile mutation is not an accepted substitute. |
| V01 | `pnpm run typecheck:build-gaps` | Not run; counts N/A. Required outcome (unverified): SDK/server prerequisites, relevant workspace typechecks and copied runtime assets valid. |
| V02 | `pnpm run test:release-registry` | Not run; counts N/A. Required outcome (unverified): All named Node release/package/install tests pass. |
| V03 | `pnpm test:run:general -- --group general-server-without-chat --shard-index I --shard-count 12` | Not run; counts N/A. Required outcome (unverified): Every I=0..11 passes. |
| V04 | `pnpm test:run:general -- --group general-chat --shard-index I --shard-count 3` | Not run; counts N/A. Required outcome (unverified): Every I=0..2 passes. |
| V05 | `pnpm test:run:general -- --group general-workspaces-a --shard-index I --shard-count 2` | Not run; counts N/A. Required outcome (unverified): Both I=0..1 pass. |
| V06 | `pnpm test:run:general -- --group general-workspaces-b` | Not run; counts N/A. Required outcome (unverified): Unsharded group passes. |
| V07 | `pnpm --filter @paperclipai/paperclip-runner check:static` | Not run; counts N/A. Required outcome (unverified): Eval kernel, TS/protocol/prep/goldens, real API authority pass. |
| V08 | `pnpm --filter @paperclipai/paperclip-runner check:runner` | Not run; counts N/A. Required outcome (unverified): Rust format/check/test/conformance/replay parity pass. |
| V09 | `pnpm --filter @paperclipai/paperclip-runner test:typescript:vitest --shard=1/2` and `--shard=2/2` | Not run; counts N/A. Required outcome (unverified): Both pass; no extra -- before --shard. |
| V10 | `pnpm --filter @paperclipai/paperclip-runner build:issue-thread`, then `pnpm build` | PARTIAL: pnpm build passed, exit 0; separate build:issue-thread prerequisite not run. [Build log](../evidence/05/build.log). |
| V11 | `docker buildx build --file .github/docker-context-checks.Dockerfile .` | Not run; counts N/A. Required outcome (unverified): Actual filtered Docker context has required manifests, traceability/replay fixtures/tests; capability contract/inventory pass. |
| V12 | `pnpm test:run:serialized -- --shard-index I --shard-count 9` | Not run; counts N/A. Required outcome (unverified): Every I=0..8 passes; selected suites serially isolated. |
| V13 | `./scripts/release.sh canary --skip-verify --dry-run` | Not run; requires CI-prepared disposable master. Fixed local refs must not be repointed. |
| V14 | `node ./scripts/e2e-shard.mjs --shard-index I --shard-count 8` | Not run; counts N/A. Required outcome (unverified): Each I=0..7 gives its declared spec argument list. |
| V15 | `PAPERCLIP_E2E_SKIP_LLM=true PAPERCLIP_PLAYWRIGHT_CHANNEL=chrome pnpm run test:e2e <V14 specs>` | Not run; counts N/A. Required outcome (unverified): All eight shards pass; retain reports/test-results. |
| Local typecheck | `pnpm -r typecheck` | PASS inside declared pnpm typecheck wrapper; exit 0. [Log](../evidence/05/typecheck.log). |
| Local full test | `pnpm test:run` | Not run on this final head; counts N/A. Do not combine targeted counts into full-suite proof. |
| Local build | `pnpm build` | PASS; exit 0; warnings retained. [Log](../evidence/05/build.log). |
| Coverage | Required100% statement coverage | Not demonstrated. The declared commands inspected do not enforce this stronger floor. |
| Post-open | CI and Greptile 5/5 with all review follow-ups resolved | Pending operator opening; no upstream PR was created. |

## Operator package

Title: **fix(logger): redact gateway credentials across log output**.

Body: [pr/05.md](../pr/05.md). This draft is not ready to submit until the named blocker and missing proof are resolved. Its unchecked local/CI/review items are intentional. The operator alone opens upstream PRs.
